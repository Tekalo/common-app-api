---
name: Build image

on:
  workflow_call:
    inputs:
      env:
        description: 'Environment to deploy to'
        type: string
        required: true
    outputs:
      image:
        description: 'Built docker image'
        value: ${{ jobs.build_and_push.outputs.image }}

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: capp/api
  DOCKER_BUILDKIT: 1

permissions:
  id-token: write
  contents: read

jobs:
  build_and_push:
    name: Build and push
    runs-on: ubuntu-latest

    outputs:
      image: ${{ steps.build-image.outputs.image }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-region: us-east-1
          role-to-assume: ${{ vars[format('{0}_AWS_PLAN_ROLE', inputs.env)] }}
          role-session-name: GithubActions
          mask-aws-account-id: false

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set short github sha variable
        run: |
          echo "GITHUB_SHA_SHORT=$(echo "$GITHUB_SHA" | cut -c 1-6)" \
            >> "$GITHUB_ENV"

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: '${{ env.GITHUB_SHA_SHORT }}'
        run: |
          docker build -t "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
            --build-arg GITHUB_SHA="$GITHUB_SHA"
            --target production \
            .
          docker push --all-tags "$ECR_REGISTRY/$ECR_REPOSITORY"
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Retrieve source maps
        run: |
          docker create --name "api-extract" "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
          docker cp extract:/api/build ./build

      - name: Create Sentry release
        uses: getsentry/action-release@v1
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ vars.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ vars.SENTRY_PROJECT }}
        with:
          environment: production
          sourcemaps: ./build
